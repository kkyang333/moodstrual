# Gemini Chatbot (FastAPI + Google Gemini API)

This is a starter project to build a chatbot using the Google Gemini API.

## 🚀 Setup

1. Clone the repo and install dependencies:
   ```bash
   pip install -r requirements.txt

